/**
 * Shared — cross-cutting code used across modules: SubmissionStatus, SessionGuard, security, and configuration.
 * Not a ticket itself; spans concerns from multiple tickets rather than owning a single entity.
 */
package com.merge.merge.shared;
