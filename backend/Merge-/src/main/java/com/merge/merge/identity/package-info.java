/**
 * Identity and Personalization — owns Student, Context, and E.Profile (Ticket 1).
 * Sharpest open question: what belongs in personalisedData, and whether it needs history.
 */
package com.merge.merge.identity;
