/**
 * Session — owns Session, including mood, type, and exhaustion handling (Ticket 6).
 * Sharpest open question: how one-active-session is enforced at the database level.
 */
package com.merge.merge.session;
